/**
 * Utilidades generales del proyecto.
 *
 * Aquí se colocarán funciones helpers compartidas.
 *
 * Ejemplos futuros:
 *   - formatDate(date: string): string
 *   - generateInvitationUrl(token: string): string
 *   - formatGuestCount(count: number): string
 */

export {};
